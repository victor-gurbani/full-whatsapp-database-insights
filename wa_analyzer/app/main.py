"""Application entrypoint.

The monolithic Streamlit body is moved here incrementally during the app
refactor. Until the final launcher flip, ``app.py`` remains the active entrypoint.
"""


def run_app() -> None:
    """Run the Streamlit app.

    This placeholder is replaced once the top-level app body has been moved out
    of ``app.py``.
    """
    raise RuntimeError("wa_analyzer.app.main.run_app is not wired yet")

